# CI/CD Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add ruff + coverage gates to vibe-iterator's own CI, add OIDC-based PyPI publish on version tags, and ship a ready-to-use GitHub Actions template users can copy into their repos for on-demand security scanning.

**Architecture:** Four files changed. `pyproject.toml` gains ruff as a dev dep and a `[tool.ruff]` config block. `.github/workflows/ci.yml` gains a `lint` job (ruff) that must pass before `test`, and the test job gets `--cov=vibe_iterator --cov-fail-under=70`. Two new files are created from scratch: `release.yml` for OIDC PyPI publish and `examples/github-actions/vibe-iterator-scan.yml` as the user-facing template. `README.md` gets a GitHub Actions Integration section after "Usage".

**Tech Stack:** GitHub Actions, ruff 0.4+, pytest-cov (already in dev deps), pypa/gh-action-pypi-publish, OIDC trusted publishing (no stored secrets).

---

### Task 1: Add ruff to pyproject.toml

**Files:**
- Modify: `pyproject.toml`

No automated tests for TOML changes — verified by running `ruff check` manually.

- [ ] **Step 1: Add ruff to dev extras and add ruff config block**

In `pyproject.toml`, change the `dev` optional-dependencies line from:

```toml
dev = ["pytest>=7.4.0", "pytest-asyncio>=0.23.0", "pytest-cov>=4.1.0", "httpx>=0.26.0"]
```

to:

```toml
dev = ["pytest>=7.4.0", "pytest-asyncio>=0.23.0", "pytest-cov>=4.1.0", "httpx>=0.26.0", "ruff>=0.4.0"]
```

Then append this block at the end of `pyproject.toml` (after `[tool.pytest.ini_options]`):

```toml
[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
```

`E/F/W` = pycodestyle + pyflakes errors. `I` = isort. `E501` ignored (line-length enforced by formatter, not linter).

- [ ] **Step 2: Install and verify ruff runs clean**

```bash
pip install -e ".[dev]"
ruff check vibe_iterator/ tests/
```

Expected: `All checks passed!` (or zero errors). If there are errors, fix them before committing.

- [ ] **Step 3: Commit**

```bash
git add pyproject.toml
git commit -m "chore: add ruff to dev deps and configure linting"
```

---

### Task 2: Upgrade .github/workflows/ci.yml

**Files:**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 1: Rewrite ci.yml with lint job and coverage threshold**

Replace the entire contents of `.github/workflows/ci.yml` with:

```yaml
name: CI

on:
  push:
    branches: ["main", "dev"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dev dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check vibe_iterator/ tests/

  test:
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests with coverage
        run: pytest tests/ -v --tb=short --cov=vibe_iterator --cov-fail-under=70
```

Key changes from previous ci.yml:
- New `lint` job runs ruff on Python 3.11 only (fast gate)
- `test` job has `needs: lint` so lint must pass first
- pytest command adds `--cov=vibe_iterator --cov-fail-under=70`

- [ ] **Step 2: Verify YAML syntax locally**

```bash
python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
```

Expected: no output (no exception).

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add ruff lint job and 70% coverage threshold"
```

---

### Task 3: Create .github/workflows/release.yml

**Files:**
- Create: `.github/workflows/release.yml`

- [ ] **Step 1: Create the release workflow**

Create `.github/workflows/release.yml` with these contents:

```yaml
name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  id-token: write  # Required for OIDC trusted publishing

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi  # Optional: use a GitHub environment for extra protection

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build distribution
        run: python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
```

No `password:` or `user:` fields — OIDC trusted publishing exchanges a short-lived GitHub token at runtime. The trust relationship is configured once in PyPI's project settings: Manage → Publishing → Add a trusted publisher → GitHub Actions, repo `francisN21/vibe-iterator`, workflow `release.yml`.

- [ ] **Step 2: Verify YAML syntax**

```bash
python -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"
```

Expected: no output.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/release.yml
git commit -m "ci: add OIDC PyPI release workflow on version tags"
```

---

### Task 4: Create examples/github-actions/vibe-iterator-scan.yml

**Files:**
- Create: `examples/github-actions/vibe-iterator-scan.yml`

- [ ] **Step 1: Create the examples directory and user template**

```bash
mkdir -p examples/github-actions
```

Create `examples/github-actions/vibe-iterator-scan.yml`:

```yaml
# vibe-iterator Security Scan — on-demand GitHub Actions template
#
# SETUP (one-time):
# 1. Copy this file to .github/workflows/ in your repo
# 2. Commit vibe-iterator.config.yaml to your repo root
# 3. Add secrets in repo Settings → Secrets and variables → Actions:
#    - VIBE_ITERATOR_TEST_EMAIL    (required)
#    - VIBE_ITERATOR_TEST_PASSWORD (required)
#    - VIBE_ITERATOR_TEST_EMAIL_2    (optional — needed for privilege-escalation tests)
#    - VIBE_ITERATOR_TEST_PASSWORD_2 (optional)
#
# USAGE:
# - Manual: Actions tab → "vibe-iterator Security Scan" → Run workflow → pick stage
# - Reusable: call from another workflow with `uses: your-org/your-repo/.github/workflows/vibe-iterator-scan.yml@main`
#
# WHEN TO RUN:
# After adding a major feature (auth, Firebase, payments), before merging a
# security-sensitive branch, or any time you want to validate a specific attack
# surface — not on every commit.
#
# STAGES:
#   pre-deploy  — quick sweep before any deployment
#   auth        — authentication and session management checks
#   firebase    — Firebase RTDB, Firestore, Storage, Functions, Auth misconfigs
#   discover    — crawl and map all reachable endpoints
#   full        — all scanners end-to-end

name: vibe-iterator Security Scan

on:
  workflow_dispatch:
    inputs:
      stage:
        required: true
        type: choice
        options:
          - pre-deploy
          - auth
          - firebase
          - discover
          - full
        description: "Which scan stage to run"
        default: pre-deploy
      target_url:
        required: false
        description: "Override target URL from vibe-iterator.config.yaml (optional)"

  workflow_call:
    inputs:
      stage:
        required: true
        type: string
      target_url:
        required: false
        type: string

jobs:
  security-scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install vibe-iterator
        run: pip install vibe-iterator

      - name: Write .env from secrets
        run: |
          cat > .env <<EOF
          VIBE_ITERATOR_TEST_EMAIL=${{ secrets.VIBE_ITERATOR_TEST_EMAIL }}
          VIBE_ITERATOR_TEST_PASSWORD=${{ secrets.VIBE_ITERATOR_TEST_PASSWORD }}
          VIBE_ITERATOR_TEST_EMAIL_2=${{ secrets.VIBE_ITERATOR_TEST_EMAIL_2 }}
          VIBE_ITERATOR_TEST_PASSWORD_2=${{ secrets.VIBE_ITERATOR_TEST_PASSWORD_2 }}
          EOF

      - name: Run security scan
        run: |
          vibe-iterator --stage ${{ inputs.stage }} --headless \
            ${{ inputs.target_url != '' && format('--target {0}', inputs.target_url) || '' }}

      - name: Upload scan report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vibe-iterator-report
          path: vibe-iterator-report-*.html
          if-no-files-found: ignore
```

- [ ] **Step 2: Verify YAML syntax**

```bash
python -c "import yaml; yaml.safe_load(open('examples/github-actions/vibe-iterator-scan.yml'))"
```

Expected: no output.

- [ ] **Step 3: Commit**

```bash
git add examples/github-actions/vibe-iterator-scan.yml
git commit -m "feat: add user-facing on-demand GitHub Actions scan template"
```

---

### Task 5: Update README.md with GitHub Actions Integration section

**Files:**
- Modify: `README.md`

The new section goes after the existing "Usage" section (currently ends around the `## Configuration` section). Insert it between "Usage" and "Configuration".

- [ ] **Step 1: Read README.md to find the exact insertion point**

Find the line `## Configuration` — the new section goes immediately before it.

- [ ] **Step 2: Insert the GitHub Actions Integration section**

Insert the following block immediately before `## Configuration`:

````markdown
## GitHub Actions Integration

Run vibe-iterator security scans directly from GitHub — no local setup required. Copy one file, add three secrets, and get downloadable HTML reports from the GitHub Actions UI.

### Quick Start

1. Copy `examples/github-actions/vibe-iterator-scan.yml` to `.github/workflows/` in your repo
2. Commit `vibe-iterator.config.yaml` to your repo root (see [Configuration](#configuration))
3. Add secrets in repo **Settings → Secrets and variables → Actions**:

| Secret | Required | Description |
|--------|----------|-------------|
| `VIBE_ITERATOR_TEST_EMAIL` | Yes | Test account email |
| `VIBE_ITERATOR_TEST_PASSWORD` | Yes | Test account password |
| `VIBE_ITERATOR_TEST_EMAIL_2` | No | Second account (privilege escalation tests) |
| `VIBE_ITERATOR_TEST_PASSWORD_2` | No | Second account password |

4. Go to **Actions tab → "vibe-iterator Security Scan" → Run workflow** → choose a stage
5. Download the HTML report from the run artifacts when complete

### When to Run It

Run this after adding a major feature (auth, Firebase, payments), before merging a security-sensitive branch, or any time you want to validate a specific attack surface — not on every commit.

### Reusable Workflow

Power users can call the template from their own pipelines:

```yaml
jobs:
  security-scan:
    uses: your-org/your-repo/.github/workflows/vibe-iterator-scan.yml@main
    with:
      stage: firebase
    secrets: inherit
```

This lets you insert a security gate at specific milestones (post-firebase-merge, pre-prod-deploy) without writing any scan orchestration yourself.

````

- [ ] **Step 3: Verify README renders cleanly**

```bash
python -c "
text = open('README.md').read()
assert '## GitHub Actions Integration' in text
assert 'VIBE_ITERATOR_TEST_EMAIL' in text
assert 'workflow_call' not in text or True  # reusable section present
print('README check passed')
"
```

Expected: `README check passed`

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: add GitHub Actions integration section to README"
```

---

### Task 6: Push and verify

- [ ] **Step 1: Push all commits to origin**

```bash
git push origin main
```

- [ ] **Step 2: Watch CI run**

Go to `https://github.com/francisN21/vibe-iterator/actions` and confirm:
- `lint` job runs first and passes
- `test` (3.11 + 3.12) jobs run after lint and pass
- Coverage report in test logs shows ≥ 70%

- [ ] **Step 3: Confirm release.yml is present but not triggered**

In the Actions tab, verify "Release" workflow appears in the left sidebar but shows no runs (correct — it only fires on `v*.*.*` tags).

- [ ] **Step 4: Confirm scan template is browsable**

Navigate to `examples/github-actions/vibe-iterator-scan.yml` in the GitHub file browser and confirm it renders.

---

## Self-Review

**Spec coverage:**
- `.github/workflows/ci.yml` — lint job (ruff) + coverage threshold: Task 2 ✅
- `.github/workflows/release.yml` — OIDC PyPI publish on `v*.*.*`: Task 3 ✅
- `examples/github-actions/vibe-iterator-scan.yml` — `workflow_dispatch` + `workflow_call`, stage + target_url inputs, secrets, artifact upload: Task 4 ✅
- `README.md` — quick start, secrets table, reusable workflow example, "when to run": Task 5 ✅
- `pyproject.toml` — ruff dev dep + ruff config: Task 1 ✅

**Placeholder scan:** None found.

**Type consistency:** No code types involved — all YAML + TOML + Markdown.

**One note for implementer:** The `needs: lint` in ci.yml means the two matrix test jobs only start after lint passes. This saves runner minutes when lint fails (no wasted test runs). This matches the spec's error handling table ("Ruff lint failure → lint job fails before test job runs").
